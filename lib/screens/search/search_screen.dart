import 'package:flutter/material.dart';

class AppSearchScreen extends StatelessWidget {
  const AppSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('App Screen Search'),
    );
  }
}
